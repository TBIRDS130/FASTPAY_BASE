/**
 * Shared Hooks
 * 
 * Generic, reusable hooks that can be used across features
 */

export { useFirebaseSync } from './useFirebaseSync'
export type {
  UseFirebaseSyncParams,
  UseFirebaseSyncReturn,
} from './useFirebaseSync'
